import stdlib.stdio
import stdlib.math




function main(args) {

    var u = x -> (x * x)
    var f = x -> x * 3

    var s = u + f

    //println(cos(2))
    //println(s(2))

    println(phase(2 + 2 * im))
    //println(pow(2, 2))
    //println(im * im)
    //println(2 * im + 2)
    //println(exp(complex(0, pi)))
    //println(pow(2, 2 * im))
    println(pow(-2, 2 * im + 2))

    println(complex(32, 32))

    var file = open("uiltes.txt", FILE_APP | FILE_BIN)
    printfln(file, 2 * im + 2)
    close(file)

    println(t(2))

    function q(s) {
        println(s)
        return s
    }

    println(q(43532543))

}



function t(q) {
    println("MANESKIN")
    return 2
}